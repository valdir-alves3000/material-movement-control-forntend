import { Container, styled } from "@mui/material";

const ListAllProductContainer = styled(Container)(() => ({
  width: "100%",
  minHeight: "calc(100vh - 200px)",
}));

export { ListAllProductContainer };
