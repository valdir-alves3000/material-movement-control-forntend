import { UpdatePosition } from "@components/Products/UpdatePosition";
import React from "react";

const update_position: React.FC = () => {
  return <UpdatePosition />;
};

export default update_position;
